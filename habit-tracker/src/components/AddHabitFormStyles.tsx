import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const StyledAddHabitForm = styled(Box)(() => ({
  display: "flex",
  flexDirection: "column",
  gap: 20,
}));

export default StyledAddHabitForm;
